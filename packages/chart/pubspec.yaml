name: chart
description: Custom animated bar chart for Qruto Budget (grouped/single series).
version: 1.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
